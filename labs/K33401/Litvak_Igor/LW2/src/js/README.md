`secret.js` is hidden from git because it contains API key

Before launching project, create `secret.js` file in current directory and create a following constant:
```js
export const apiKey = "YOUR_KEY_VALUE"
```
